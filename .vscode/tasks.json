{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "configure",
            "type": "shell",
            "command": "cmake -S. -Bbuild -DCMAKE_INSTALL_PREFIX=build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "build:release",
            "type": "shell",
            "command": "cmake --build ./build --config Release -- -j 16",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "build:debug",
            "type": "shell",
            "command": "cmake --build ./build --config Debug -- -j 16",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "pack:release",
            "type": "shell",
            "command": "cmake --build ./build --config Release --target pack -- -j 16",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "pack:debug",
            "type": "shell",
            "command": "cmake --build ./build --config Debug --target pack -- -j 16",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        }
    ]
}
